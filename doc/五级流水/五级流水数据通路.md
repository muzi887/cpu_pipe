下面是根据你的**微程序控制单总线模型机**（PC/MAR/MDR/IR/AC/AX/BX/CX + ALU + PSW + RAM + `microController`）改造成的**五级流水数据通路**详细 Mermaid 示意图。核心思路是：**去掉单总线分时复用，改为段间专用通路 + 流水线寄存器 + 硬布线控制器**。

---

## 1. 总体架构：旧 → 新 映射

```mermaid
flowchart LR
    subgraph OLD["原多周期微程序 CPU"]
        direction TB
        MC["microController<br/>CM_ROM 微程序"]
        MC -->|"microCommands<br/>LoadPC/LoadMAR/LoadMDR/..."| BUS["internal_bus<br/>单总线 + Bus_Sel MUX"]
        BUS --- PC0["PC"]
        BUS --- MAR0["MAR"]
        BUS --- MDR0["MDR"]
        BUS --- IR0["IR"]
        BUS --- AC0["AC"]
        BUS --- AX0["AX / BX / CX"]
        AC0 --> ALU0["ALU"]
        ALU0 --> PSW0["PSW_Z"]
        MAR0 --> RAM0["main_memory"]
        MDR0 <--> RAM0
        IR0 --> MC
        PSW0 --> MC
    end

    subgraph NEW["五级流水 RISC CPU"]
        direction TB
        IF1["IF 取指"] --> IFID["IF/ID"]
        IFID --> ID1["ID 译码"]
        ID1 --> IDEX["ID/EX"]
        IDEX --> EX1["EX 执行"]
        EX1 --> EXMEM["EX/MEM"]
        EXMEM --> MEM1["MEM 访存"]
        MEM1 --> MEMWB["MEM/WB"]
        MEMWB --> WB1["WB 写回"]
        CU["control_unit<br/>硬布线译码"]
        HZ["hazard_unit<br/>stall / flush"]
        FW["forwarding_unit<br/>数据转发"]
        CU -.-> ID1
        HZ -.-> IF1 & ID1
        FW -.-> EX1
    end

    OLD -.->|"改造"| NEW

    style OLD fill:#fff3e0
    style NEW fill:#e8f5e9
```

| 原组件 | 原作用 | 流水化后归属 |
|--------|--------|--------------|
| `PC_reg` + `LoadPC`/`IncPC` | 取指/跳转 | **IF** 级 PC；分支在 **EX** 更新 |
| `MAR` + `MemR` | 送地址读 RAM | **IF** 用 PC 读指令；**MEM** 用 ALU 结果访存 |
| `MDR` + `MDR_Sel` | 缓冲 RAM 数据 | **IF/ID.instr**；**MEM/WB.mem_data** |
| `IR_reg` + `LoadIR` | 锁存指令 | **IF/ID** 流水线寄存器 |
| `IR(15:12)` → Map | 微程序跳转 | **ID** 硬布线 `control_unit` |
| `IR_Operand_Ext` | 立即数扩展 | **ID** 立即数扩展器 |
| `AC` + `AC_Sel` + ALU | 运算 | **EX** ALU + **WB** 写回 |
| `AX/BX/CX` | 专用寄存器 | 统一 **reg_file**（x0–x15） |
| `PSW_Z` + `JNZ` | 条件分支 | **EX** 比较 + `BNE` |
| `internal_bus` + `Bus_Sel` | 分时传数 | **段间专用线 + Forward MUX** |
| `microController` | 微周期控制 | **control_unit + 控制信号流水传递** |

---

## 2. 五级流水数据通路（详细）

```mermaid
flowchart TB
    subgraph CTRL["全局控制与冒险"]
        CU["control_unit<br/>输入: IF/ID.instr<br/>输出: RegWrite, MemRead, MemWrite,<br/>MemToReg, ALUSrc, ALUOp, Branch, Jump"]
        HZ["hazard_unit"]
        FW["forwarding_unit<br/>ForwardA / ForwardB"]
        CU --- HZ
        CU --- FW
    end

    subgraph IF_STAGE["① IF 取指阶段"]
        direction TB
        PC["PC_reg<br/>16-bit"]
        PCADD["PC + 1"]
        IMEM["指令存储器 / main_memory<br/>InstrMemRead"]
        PC -->|"address"| IMEM
        PC --> PCADD
        PCADD -->|"pc_plus1"| IFID_REG
        IMEM -->|"instruction 16b"| IFID_REG
        PC --> IFID_REG
    end

    subgraph IFID["IF/ID 流水线寄存器"]
        IFID_REG["{ pc, pc_plus1, instr }<br/>flush → NOP"]
    end

    subgraph ID_STAGE["② ID 译码 / 读寄存器阶段"]
        direction TB
        DEC["指令字段拆分<br/>opcode | rs1 | rs2 | rd | imm/offset"]
        IMM["立即数扩展<br/>零扩展 / 符号扩展"]
        RF_R["reg_file 读端口<br/>ReadData1 ← rs1<br/>ReadData2 ← rs2"]
        IFID_REG --> DEC
        DEC --> IMM
        DEC --> RF_R
        DEC --> CU
        IMM --> IDEX_REG
        RF_R --> IDEX_REG
        DEC --> IDEX_REG
        CU -->|"control 锁入"| IDEX_REG
    end

    subgraph IDEX["ID/EX 流水线寄存器"]
        IDEX_REG["{ pc, rs1_val, rs2_val,<br/>rs1, rs2, rd, imm, control }<br/>stall → bubble"]
    end

    subgraph EX_STAGE["③ EX 执行阶段"]
        direction TB
        FWMUXA["Forward MUX A<br/>00=ID/EX.rs1_val<br/>01=EX/MEM.alu_result<br/>10=MEM/WB.write_data"]
        FWMUXB["Forward MUX B<br/>同上，选 rs2 / imm"]
        ALUSRC["ALUSrc MUX<br/>0=寄存器 1=立即数"]
        ALU["ALU<br/>ADD / SUB / AND<br/>输出 alu_result + Zero"]
        BRADD["分支目标<br/>branch_target = pc + imm"]
        BRCMP["分支判断<br/>branch_taken = Branch ∧ (rs1≠rs2)<br/>替代 PSW_Z + JNZ"]
        IDEX_REG --> FWMUXA & FWMUXB
        FW --> FWMUXA & FWMUXB
        FWMUXA --> ALU
        FWMUXB --> ALUSRC --> ALU
        IDEX_REG --> ALUSRC
        IDEX_REG --> BRADD
        FWMUXA & FWMUXB --> BRCMP
        ALU --> EXMEM_REG
        IDEX_REG --> EXMEM_REG
        BRCMP --> EXMEM_REG
        BRADD --> EXMEM_REG
    end

    subgraph EXMEM["EX/MEM 流水线寄存器"]
        EXMEM_REG["{ alu_result, rs2_val, rd,<br/>branch_taken, branch_target, control }"]
    end

    subgraph MEM_STAGE["④ MEM 访存阶段"]
        direction TB
        MAR_PIPE["访存地址 = alu_result<br/>原 MAR ← bus，现由 EX 直接给出"]
        MEMCTRL["MemRead / MemWrite"]
        DMEM["main_memory / D-Cache<br/>DataRead / DataWrite"]
        EXMEM_REG -->|"alu_result → Addr"| MAR_PIPE
        MAR_PIPE --> DMEM
        EXMEM_REG -->|"rs2_val → WriteData"| DMEM
        EXMEM_REG --> MEMCTRL --> DMEM
        DMEM -->|"load_data"| MEMWB_REG
        EXMEM_REG --> MEMWB_REG
    end

    subgraph MEMWB["MEM/WB 流水线寄存器"]
        MEMWB_REG["{ mem_data, alu_result, rd, control }"]
    end

    subgraph WB_STAGE["⑤ WB 写回阶段"]
        direction TB
        WBMUX["MemToReg MUX<br/>0=alu_result 1=mem_data<br/>替代 AC_Sel / LoadAC"]
        RF_W["reg_file 写端口<br/>WriteData → rd<br/>RegWrite 使能"]
        MEMWB_REG --> WBMUX --> RF_W
        MEMWB_REG --> RF_W
    end

    subgraph PC_UPDATE["PC 更新（分散在 IF / EX）"]
        PCMUX["PC 选择 MUX<br/>0=pc_plus1 1=branch_target 2=jump_target"]
        PCADD --> PCMUX
        EXMEM_REG -->|"branch_taken"| PCMUX
        EXMEM_REG -->|"branch_target"| PCMUX
        HZ -->|"stall: 冻结 PC"| PC
        HZ -->|"flush: IF/ID←NOP"| IFID_REG
        PCMUX --> PC
    end

    RF_W -->|"写回"| RF_R
    EXMEM_REG -.->|"Forward 01"| FWMUXA
    EXMEM_REG -.->|"Forward 01"| FWMUXB
    MEMWB_REG -.->|"Forward 10"| FWMUXA
    MEMWB_REG -.->|"Forward 10"| FWMUXB

    IF_STAGE --> IFID --> ID_STAGE --> IDEX --> EX_STAGE --> EXMEM --> MEM_STAGE --> MEMWB --> WB_STAGE

    style IF_STAGE fill:#e3f2fd
    style ID_STAGE fill:#f3e5f5
    style EX_STAGE fill:#fff8e1
    style MEM_STAGE fill:#fce4ec
    style WB_STAGE fill:#e8f5e9
    style IFID fill:#bbdefb
    style IDEX fill:#bbdefb
    style EXMEM fill:#bbdefb
    style MEMWB fill:#bbdefb
```

---

## 3. 原微周期 → 五级流水 时序对照

以原取指微序列 `PC→MAR→RAM→MDR→IR` 和 `MOVE AC,[BX]` 为例：

```mermaid
sequenceDiagram
    autonumber
    participant T as 时钟周期
    participant IF as IF
    participant ID as ID
    participant EX as EX
    participant MEM as MEM
    participant WB as WB

    Note over IF,WB: 周期1：指令 i 进入 IF（PC→Mem→instr 锁入 IF/ID）
    T->>IF: PC→I-Mem, instr→IF/ID

    Note over IF,WB: 周期2：i 在 ID；i+1 在 IF
    T->>ID: 译码 i, 读 reg_file, 产生 control
    T->>IF: 取 i+1

    Note over IF,WB: 周期3：i 在 EX；i+1 在 ID；i+2 在 IF
    T->>EX: ALU 运算 / 分支比较
    T->>ID: 译码 i+1
    T->>IF: 取 i+2

    Note over IF,WB: 周期4：i 在 MEM（原 MAR+MDR+RAM 阶段）
    T->>MEM: Addr=alu_result, MemRead/Write
    T->>EX: i+1 执行
    T->>ID: i+2 译码
    T->>IF: 取 i+3

    Note over IF,WB: 周期5：i 在 WB（原 LoadAC/LoadBX/LoadCX）
    T->>WB: 写 reg_file（RegWrite）
    T->>MEM: i+1 访存
    T->>EX: i+2 执行
    T->>ID: i+3 译码
    T->>IF: 取 i+4
```

**对比原模型机**：原来一条指令要多个微周期串行占用总线；流水化后 5 个阶段**并行**，每拍各处理不同指令的不同阶段。

---

## 4. 冒险处理单元（必须加）

单总线模型机天然串行、无冒险；流水化后必须补这三块：

```mermaid
flowchart TB
    subgraph HAZ["hazard_unit + forwarding_unit"]
        subgraph RAW_FWD["RAW 转发（ALU→ALU）"]
            F1["检测: EX/MEM.rd = ID/EX.rs1/rs2<br/>且 RegWrite=1"]
            F2["检测: MEM/WB.rd = ID/EX.rs1/rs2<br/>且 RegWrite=1"]
            F1 --> FA["ForwardA/B = 01<br/>选 EX/MEM.alu_result"]
            F2 --> FB["ForwardA/B = 10<br/>选 MEM/WB.write_data"]
        end

        subgraph LOAD_USE["Load-Use Stall"]
            LU["检测: ID/EX=LD 且 rd=下条 rs1/rs2"]
            LU --> ST["stall 1 拍:<br/>PC_hold, IF/ID_hold,<br/>ID/EX ← bubble"]
        end

        subgraph BRANCH["控制冒险（原 JNZ）"]
            BR["EX: branch_taken = Branch ∧ (rs1≠rs2)"]
            BR --> FL["flush:<br/>PC ← branch_target<br/>IF/ID ← NOP<br/>ID/EX ← NOP"]
        end
    end

    style RAW_FWD fill:#e8f5e9
    style LOAD_USE fill:#fff3e0
    style BRANCH fill:#ffebee
```

---

## 5. 控制信号流水传递（替代 microCommands）

原 32 位 `microCommands` 每位控制一个微操作；流水 CPU 改为**ID 一次译码，控制信号随流水线寄存器向后传**：

```mermaid
flowchart LR
    INSTR["IF/ID.instr"] --> CU["control_unit"]
    CU -->|"RegWrite, MemRead, MemWrite,<br/>MemToReg, ALUSrc, ALUOp,<br/>Branch, Jump"| IDEX_C["ID/EX.control"]
    IDEX_C --> EXMEM_C["EX/MEM.control"]
    EXMEM_C --> MEMWB_C["MEM/WB.control"]

    IDEX_C -.->|"ALUSrc, ALUOp"| EX_USE["EX 级 ALU"]
    EXMEM_C -.->|"MemRead, MemWrite"| MEM_USE["MEM 级访存"]
    MEMWB_C -.->|"RegWrite, MemToReg"| WB_USE["WB 级写回"]

    style CU fill:#f3e5f5
```

| 原微命令 | 流水化后等价 |
|----------|--------------|
| `LoadMAR` + `MemR` + `LoadMDR` + `LoadIR` | IF 级一次完成，结果进 **IF/ID** |
| `Bus_Sel` 选 IR/Operand | ID 级 **imm 扩展 + reg_file 读** |
| `LoadAC` + `ALU_op` + `AC_Sel←ALU` | EX 级 **ALU** + WB 级 **RegWrite** |
| `LoadAX/BX/CX`, `IncAX/IncBX/DecCX` | WB 级 **RegWrite**（ADDI/ADD 实现） |
| `MemW` + MDR→RAM | MEM 级 **MemWrite** |
| `LoadPC` + `PC_Sel` | IF 级 **PC+1**；EX 级 **branch_taken→PC** |
| `LoadPSW` + `JNZ` | EX 级 **BNE 比较**，不再单独 PSW 寄存器 |

---

## 6. 寄存器堆整合（AX/BX/CX/AC → reg_file）

```mermaid
flowchart LR
    subgraph OLD_REG["原分散寄存器"]
        AC["AC 累加器"]
        AX["AX 地址/结果"]
        BX["BX 基址指针"]
        CX["CX 循环计数"]
    end

    subgraph NEW_REG["流水 reg_file x0–x15"]
        x0["x0 = 0"]
        x1["x1 = a"]
        x2["x2 = b"]
        x3["x3 = tmp"]
        x4["x4 = ptr"]
        x5["x5 = cnt"]
        x6["x6… 扩展"]
    end

    AC -.->|"运算中间值"| x3
    AX -.->|"写地址"| x4
    BX -.->|"读指针"| x4
    CX -.->|"循环计数"| x5

    OLD_REG --> NEW_REG
```

---

## 设计要点小结

1. **单总线消失**：原 `Bus_Sel` 在 5 个时钟里选 PC/MDR/IR/AC…；流水 CPU 用**固定连线**，冲突靠 **Forwarding MUX** 解决。
2. **MAR/MDR 不再独立可见**：IF 级隐含“PC 即地址”；MEM 级 `alu_result` 即 MAR，`mem_data` 即原 MDR 读回值。
3. **微程序控制器退役**：`microController` → `control_unit`；微周期序列 → 固定 5 级 + 冒险处理。
4. **分支提前到 EX**：原 `JNZ` 在微程序末尾看 `PSW_Z`；流水 CPU 在 EX 比较 `rs1/rs2`，误取 2 条需 **flush**。
5. **Load-Use 必须 stall 1 拍**：原模型机 `MOVE AC,[BX]` 下一微步才能用 AC；流水里 LD 结果要到 MEM 末才就绪，EX 太早，转发解决不了。

如果你需要，我可以把上述 Mermaid **直接写入** `流水线CPU-Cache-中断-嵌入式课设-初期报告.md` 的 §5 章节，或再出一版**仅保留 IF/ID/EX/MEM/WB、适合 PPT 的精简图**。